# 互換のため残しています。バックエンド Dockerfile は uv.lock 由来の git+https で
# mfer-tools / inference-ecg-bnp を取得するため、SSH マウントは必須ではありません。
# 将来、Dockerfile 内で `RUN --mount=type=ssh` を使う場合に `docker compose build --ssh default` を付けてください。
#
# 使い方（従来どおり）:
#   make dev-ssh / make build-ssh
services:
  backend:
    build:
      ssh:
        - default
